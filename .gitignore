# Secrets — never commit these
.env

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store

# NOTE: we intentionally DO commit recordings/ and transcripts/
# because the challenge requires them as deliverables.
